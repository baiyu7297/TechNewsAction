name: Daily AI Tech News Push

on:
  schedule:
    # 每天早上8点 (UTC时间 0点 = 北京时间8点)
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许手动触发

jobs:
  fetch-and-push-ai-news:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install dependencies
      run: npm install
      
    - name: Health check
      run: npm run health
      
    - name: Fetch AI news and push
      env:
        # 企业微信配置
        WECHAT_WEBHOOK: ${{ secrets.WECHAT_WEBHOOK }}
        WECHAT_APP_ID: ${{ secrets.WECHAT_APP_ID }}
        WECHAT_APP_SECRET: ${{ secrets.WECHAT_APP_SECRET }}
        # Server酱配置
        SERVER_CHAN_KEY: ${{ secrets.SERVER_CHAN_KEY }}
        # 钉钉配置
        DINGTALK_WEBHOOK: ${{ secrets.DINGTALK_WEBHOOK }}
        # 邮件配置（Gmail + nodemailer）
        SMTP_USER: ${{ secrets.SMTP_USER }}
        SMTP_PASS: ${{ secrets.SMTP_PASS }}
        TO_EMAIL: ${{ secrets.TO_EMAIL }}
        # 备用邮件配置（SendGrid API）
        SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
        # AI 翻译配置
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        OPENAI_MODEL: ${{ vars.OPENAI_MODEL }}
        TRANSLATE_TO_ZH: ${{ vars.TRANSLATE_TO_ZH }}
        # 可选筛选参数
        NEWS_LOOKBACK_HOURS: ${{ vars.NEWS_LOOKBACK_HOURS }}
        NEWS_FALLBACK_DAYS: ${{ vars.NEWS_FALLBACK_DAYS }}
        NEWS_MAX_ITEMS: ${{ vars.NEWS_MAX_ITEMS }}
        NEWS_MIN_ITEMS: ${{ vars.NEWS_MIN_ITEMS }}
      run: node src/index.js
      
    - name: Upload logs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: execution-logs
        path: logs/
        retention-days: 7
