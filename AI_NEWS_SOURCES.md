# AI 新闻源说明

## 📰 当前新闻源

### 1. 机器之心 (jiqizhixin.com)
- **简介**: 专业的人工智能媒体和产业服务平台
- **特点**: 
  - 深度报道 AI 技术进展
  - 覆盖学术界和产业界
  - 提供论文解读和技术分析
- **内容类型**: 技术文章、论文解读、产业动态、人物访谈

### 2. 量子位 (qbitai.com)
- **简介**: 关注人工智能及前沿科技的专业媒体
- **特点**:
  - 快速报道 AI 领域最新动态
  - 关注大模型、自动驾驶等热点
  - 风格轻松易读
- **内容类型**: 新闻快讯、技术解读、行业分析

### 3. 雷峰网 AI 科技评论 (leiphone.com/category/ai)
- **简介**: 雷峰网旗下的 AI 专栏
- **特点**:
  - 深度技术评论
  - 产业趋势分析
  - 企业案例研究
- **内容类型**: 深度报道、技术评测、行业观察

## 🎯 为什么选择这些新闻源

1. **专业性强** - 都是国内知名的 AI 技术媒体
2. **更新及时** - 每天都有新内容发布
3. **中文内容** - 适合中文读者阅读
4. **质量保证** - 内容经过编辑审核，质量有保障
5. **覆盖全面** - 从学术研究到产业应用都有涉及

## 📊 内容覆盖范围

- 🧠 **大语言模型**: GPT、Claude、文心一言等
- 🚗 **自动驾驶**: 特斯拉、小鹏、理想等
- 🤖 **机器人**: 人形机器人、工业机器人
- 🎨 **AI 绘画**: Midjourney、Stable Diffusion、DALL-E
- 🔬 **学术研究**: 最新论文解读、技术突破
- 💼 **产业动态**: 融资消息、产品发布、政策法规
- 👥 **人物访谈**: AI 领域专家、创业者

## 🔄 如何添加新的新闻源

如果你想添加其他 AI 新闻源，可以编辑 `src/newsFetcher.js` 文件：

### 步骤 1: 添加新的获取函数

```javascript
async fetchYourSource() {
  try {
    console.log('正在从 你的新闻源 获取新闻...');
    const response = await axios.get('https://your-source.com/', {
      headers: {
        'User-Agent': 'Mozilla/5.0 ...',
        'Accept-Language': 'zh-CN,zh;q=0.9'
      },
      timeout: 15000
    });

    const $ = cheerio.load(response.data);
    const news = [];
    
    // 根据网站结构调整选择器
    $('.article-item').each((index, element) => {
      if (index >= 10) return false;
      
      const $element = $(element);
      const title = $element.find('.title').text().trim();
      let link = $element.find('a').attr('href');
      
      if (title && link) {
        if (!link.startsWith('http')) {
          link = `https://your-source.com${link}`;
        }
        
        news.push({
          title: title,
          link: link,
          source: '你的新闻源',
          time: moment(),
          category: 'AI'
        });
      }
    });
    
    return news;
  } catch (error) {
    console.error('❌ 获取失败:', error.message);
    return [];
  }
}
```

### 步骤 2: 在 fetchAllNews 中调用

```javascript
async fetchAllNews() {
  const results = await Promise.allSettled([
    this.fetchJiQiZhiXin(),
    this.fetchQBitAI(),
    this.fetchAITechReview(),
    this.fetchYourSource()  // 添加你的新闻源
  ]);
  
  // 更新 sources 数组
  const sources = ['机器之心', '量子位', '雷峰网AI', '你的新闻源'];
  // ...
}
```

## 🛠️ 调试技巧

### 1. 查看网页结构
使用浏览器开发者工具（F12）查看目标网站的 HTML 结构，找到合适的选择器。

### 2. 测试选择器
可以在浏览器控制台中测试 CSS 选择器：
```javascript
document.querySelectorAll('.article-item')
```

### 3. 处理动态内容
如果网站使用 JavaScript 动态加载内容，可能需要：
- 查找 API 接口
- 使用 Puppeteer 等工具渲染页面
- 寻找 RSS 源

### 4. 本地测试
在提交前先本地测试：
```bash
npm start
```

## 📝 注意事项

1. **尊重版权** - 只获取标题和链接，不抓取全文
2. **控制频率** - 不要过于频繁地请求，避免给网站造成压力
3. **错误处理** - 确保有完善的错误处理，单个源失败不影响其他源
4. **User-Agent** - 设置合理的 User-Agent，表明是自动化工具
5. **超时设置** - 设置合理的超时时间，避免长时间等待

## 🔮 未来计划

可能添加的新闻源：
- [ ] 新智元
- [ ] AI前线
- [ ] InfoQ AI
- [ ] 36氪 AI 专栏
- [ ] 虎嗅 AI
- [ ] PingWest品玩 AI

如果你有好的建议，欢迎提 Issue 或 PR！
